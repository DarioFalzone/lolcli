click>=8.0.0
requests>=2.25.0

# Database & ORM
sqlalchemy>=2.0.0

# API Backend
fastapi>=0.109.0
uvicorn[standard]>=0.27.0
pydantic>=2.0.0
python-multipart>=0.0.6

# Frontend
jinja2>=3.1.0

# Image processing (extracción de paleta de colores en cli.py)
Pillow>=10.0.0

# Runtime Utils
python-dotenv>=1.0.0

# Meta Scraper (puerto 8002)
# Requerido por src/riot_lol_cli/meta_scraper/adapters/{opgg,ugg,lolalytics}.py.
# Después de `pip install playwright` correr: `playwright install chromium`.
# El servidor levanta sin Playwright (los adapters fallan con mensaje claro).
playwright>=1.40.0

# Patch Notes V2 (puerto 8005)
# beautifulsoup4: parsing HTML estático del calendario Riot y adapters comunidad.
# apscheduler: cron in-process opcional (LOLCLI_PATCH_NOTES_CRON_ENABLED=1).
beautifulsoup4>=4.12.0
apscheduler>=3.10.0

# Patch Notes -> MongoDB Atlas export (opcional)
# Solo necesario para scripts/export_patch_notes_to_mongo.py.
# El script aborta con exit code 2 si pymongo no está instalado.
pymongo>=4.6,<5
